1.usertrap()函数调用了syscall()
2.p/x p->trapframe->a7,打印出来的结果是0xf
3.用户态
4.80001cf8: 00002683           lw      a3,0(zero)
  num对应于寄存器a3
5.内核崩溃是因为在内核态解引用空指针，访问了未映射的地址 0，导致 MMU 转换失败并触发了“加载页错误”（scause=0xd），最终被内核陷阱处理程序判定为严重错误而停机。
6.pid=1，进程名称是init